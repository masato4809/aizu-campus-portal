#!/usr/bin/env bash
set -euo pipefail

# Run php inside the dev container.
repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

exec docker compose exec -T -w /var/www/html/wportal2-app wportal2_web php "$@"
